{
  "name": "@openclaw/workflow-orchestrator",
  "version": "0.1.0",
  "description": "Workflow orchestration plugin - Phase 1 MVP: Subagent tracking + message interception",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "dev": "tsx watch src/plugin.ts",
    "watch": "tsc --watch",
    "clean": "rm -rf dist .openclaw",
    "lint": "echo 'Linting skipped for Phase 1'"
  },
  "keywords": [
    "openclaw",
    "plugin",
    "workflow",
    "orchestrator",
    "subagent-tracking"
  ],
  "author": "OpenClaw",
  "license": "MIT",
  "openclaw": {
    "extensions": ["./dist/index.js"]
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^22.0.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
